# Keep one lockfile for the whole monorepo (this is pnpm's default, but making it explicit is fine)
shared-workspace-lockfile=true

# Force explicit "workspace:" deps when you want local packages.
# This avoids pnpm's "link if version range matches" behavior, which pnpm documents as introducing uncertainty.
link-workspace-packages=false
